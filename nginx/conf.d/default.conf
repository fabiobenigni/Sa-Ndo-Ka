# Upstream per l'applicazione Next.js
upstream app {
    server app:3000;
}

# Redirect HTTP to HTTPS (commenta se non usi HTTPS)
# server {
#     listen 80;
#     server_name ${DOMAIN:-localhost};
#     return 301 https://$server_name$request_uri;
# }

# Server HTTP (usa questo se non configuri HTTPS)
# Accetta connessioni da qualsiasi host (localhost, IP, nome host)
server {
    listen 80;
    # _ è un catch-all che accetta qualsiasi hostname
    server_name _;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Proxy settings
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;

    # Timeouts
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    # Max body size per upload
    client_max_body_size 50M;

    # Root location
    location / {
        proxy_pass http://app;
    }

    # Health check endpoint
    location /health {
        access_log off;
        proxy_pass http://app/api/health;
    }
}

# Server HTTPS (decommenta e configura se usi HTTPS)
# server {
#     listen 443 ssl http2;
#     server_name ${DOMAIN:-localhost};
#
#     # SSL certificates (Let's Encrypt)
#     ssl_certificate /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     # Logging
#     access_log /var/log/nginx/access.log;
#     error_log /var/log/nginx/error.log;
#
#     # Proxy settings
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection 'upgrade';
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_cache_bypass $http_upgrade;
#
#     # Timeouts
#     proxy_connect_timeout 60s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
#
#     # Max body size per upload
#     client_max_body_size 50M;
#
#     # Root location
#     location / {
#         proxy_pass http://app;
#     }
#
#     # Health check endpoint
#     location /health {
#         access_log off;
#         proxy_pass http://app/api/health;
#     }
# }

